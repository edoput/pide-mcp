---
schema: isabelle-mcp.plan/v1
id: context_locator
title: Root tool catalogue and ML-owned invocation targets
status: in_progress
depends_on:
- connection_kernel
allowed_modules:
- spec
- plans
- CHANGELOG
- README
- mcp/src
- mcp/Tools
- mcp_test/src
- mcp/test
invariants:
- id: INV1
  statement: Every accepted context locator is parsed and resolved only by Isabelle/ML
    and yields one ordinary Proof.context at the point an operation executes.
- id: INV2
  statement: Context-resolver kinds are immutable theory declarations inherited through
    imports; duplicate definitions cannot silently replace one another.
- id: INV3
  statement: Scala transports a target URL as an opaque string and does not branch
    on resolver kinds.
- id: INV4
  statement: No MCP request can include, open, or otherwise apply an Isabelle bundle;
    bundle activation is controlled only by Isabelle theory and proof text.
claims:
- id: A1
  kind: assumption
  state: active
  statement: Theory_Data attached to a registry-root theory inherits resolver declarations
    from its theory imports and can safely store the resolver functions needed to
    construct Proof.context values.
  layers:
  - ml-unit
- id: A2
  kind: assumption
  state: active
  statement: A loaded theory produces a global Proof.context when its target is resolved.
  layers:
  - ml-unit
  - bridge
- id: A3
  kind: assumption
  state: active
  statement: The server can bootstrap a private live PIDE wrapper importing exactly
    the -T selected theory, hold its document requirement until shutdown, and observe
    ancestor/wrapper reprocessing without rebuilding Scala.
  layers:
  - bridge
- id: I1
  kind: infrastructure
  state: active
  statement: MCP_Context_Locator is the sole owner of locator parsing, canonical printing,
    resolver registration, and resolution to Proof.context.
  layers:
  - ml-unit
  - bridge
- id: D1
  kind: design
  statement: A context locator is an address for an existing Isabelle context, not
    a third context kind beside theory, Proof.context, and Context.generic.
  category: design
- id: D2
  kind: design
  statement: Scala transports target URLs opaquely; ML owns the current parser and
    resolver extension mechanism. Do not redesign resolver result types in this cleanup.
  category: design
- id: D3
  kind: design
  statement: MCP_Context_Locator stores named resolver functions in Theory_Data on
    the registry-root theory; MCP_Tools registers the theory resolver through ordinary
    setup.
  category: design
- id: D4
  kind: design
  statement: Every startup creates one private temporary live PIDE wrapper importing
    exactly the -T theory, uniformly for image and source imports. Its context supplies
    the catalogue and default execution. Bootstrap/final end precede hello/Ready;
    lifetime document retention ends only after drain, release and stop. No source/image
    split or heap fallback is allowed.
  category: design
- id: D5
  kind: design
  statement: The root catalogue selects names, activation and actual ML tool implementations;
    an invocation target supplies only the execution Proof.context.
  category: design
- id: D6
  kind: design
  statement: 'Every ML tool automatically advertises optional String context in ordinary
    arguments. The framework resolves its URL in ML, strips it before invoking unchanged
    #run, and defaults to root when absent. A tool-owned context declaration fails
    clearly; Scala builtins do not gain this parameter.'
  category: design
- id: Q1
  kind: question
  state: resolved
  statement: May an MCP client change the registry-root theory after the connection
    becomes Ready?
  resolution: No in this plan. The root is runtime startup configuration and is immutable
    for one active connection; a future control-plane plan may define atomic switching,
    in-flight snapshot ownership, readiness revalidation, and catalog notifications.
  resolved_on: '2026-08-31'
- id: T1
  kind: test
  state: active
  statement: Theory and extension locators round-trip through the existing ML parser/printer;
    malformed forms fail before resolver invocation.
  layers:
  - ml-unit
- id: T2
  kind: test
  state: active
  statement: A resolver registered in an imported theory is available in its descendant,
    an unrelated theory cannot use it, and duplicate resolver-kind registration fails
    at theory construction or merge.
  layers:
  - ml-unit
- id: T3
  kind: test
  state: active
  statement: Theory and extension targets resolve at execution; unknown kinds and
    unknown theories return operation errors.
  layers:
  - ml-unit
  - bridge
- id: T4
  kind: test
  state: active
  statement: ML tool schemas include optional String context, builtin schemas do not;
    declaring a tool-owned context parameter fails. Scala transports extension URLs
    opaquely and ML rejects malformed/unavailable targets and removes context before
    invoking the tool.
  layers:
  - scala-unit
  - bridge
  - ml-unit
- id: T5
  kind: test
  state: active
  statement: tools/list uses the fresh root catalogue; tools/call selects the root
    tool value and executes it in the target, including when the target declares a
    conflicting same-name tool. No request accepts bundle activation.
  layers:
  - scala-unit
  - ml-unit
  - bridge
  - e2e
- id: T6
  kind: test
  state: active
  statement: Starting with -T Scratch owns a private live PIDE wrapper importing exactly
    Scratch until shutdown, observes successful final end before readiness, and sees
    changed inherited tools after ancestor/wrapper reprocessing. Image and source
    selections use the same wrapper path; invocation targets never change its catalogue.
  layers:
  - bridge
  - e2e
spec_refinements: []
done_command:
- tools/planning-gate
- done
---






The private live wrapper imports exactly the -T selected theory and owns the
catalogue, operation registry and resolver registry. Its Proof.context is the
default execution context, uniformly for image and source selections.
ML resolves the invocation target and calls the selected root tool value in that
Proof.context. Keep the existing resolver API and extension direction, including
future URL schemes. No connection-selected context, overlays or richer state
results are introduced. Notifications signal a refresh; imports merge ancestor
data into descendants and do not propagate declarations upwards.

Every internal hello/call carries mandatory root_node, root_command and root_exec
attributes alongside the existing theory identity. The node names the owned
wrapper; command and evaluation IDs select its completed final end. Use existing
Isabelle snapshot/Document APIs only, with no prover-source patches. ML checks
current execution identity, completion, final-end state and theory identity before
selecting the root. Missing, malformed, stale or unfinished selectors fail through
correlated errors, without fallback or automatic retry. Replies, cancel/drain,
operation payloads and public context arguments remain unchanged.
The wrapper requirement lasts from startup to shutdown. Bootstrap and successful
final end precede Ready; drain precedes release/stop. This is one server-owned
root, not a multi-user document service.
