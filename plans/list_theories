list_theories — retained server obligations
==================================================

Status: retained contract; current carve-out verification pending.

List sorted theory names for the requested session from the existing catalogue. Do not add tier, source-position or filtering features.

Verification obligations
------------------------

T1 [bridge] [scala-unit] For a known session, list_theories returns its sorted catalogue theory names.

T2 [scala-unit] Unknown session returns an error naming it; a known session with no theories returns an empty successful report.

T3 [scala-unit] The returned names match the configured session catalogue, without promising tier or source-location fields.

T4 [e2e] Public list_theories uses the requested session and returns its theory names.
