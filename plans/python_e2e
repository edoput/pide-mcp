---
schema: isabelle-mcp.plan/v1
id: python_e2e
title: Register deterministic procedural end-to-end tests
status: in_progress
depends_on:
- verification_matrix
allowed_modules:
- mcp/test
- tools/planning_gate
invariants:
- id: INV1
  statement: End-to-end discovery executes no test body and starts no Isabelle or
    MCP process.
- id: INV2
  statement: Every registered end-to-end worker, client process group, and descendant
    is reaped on success, failure, timeout, or interruption.
claims:
- id: A1
  kind: assumption
  state: active
  statement: A procedural process test exercises the shipped process and public protocol,
    not an in-process fake or an LLM agent.
  layers:
  - e2e
- id: A2
  kind: assumption
  state: active
  statement: Metadata discovery is deterministic and has no process, network, prover,
    or test-body side effects.
  layers:
  - tooling-unit
- id: I1
  kind: infrastructure
  state: active
  statement: All cases share one client, timeout, teardown, metadata, and result-reporting
    implementation instead of copying process code between scripts.
  layers:
  - tooling-unit
- id: D1
  kind: design
  statement: Agent usability is a separate evaluation concern; process-test links
    certify only deterministic procedural behavior.
  category: design
- id: D2
  kind: design
  statement: Each worker owns a process session and each JSON-RPC client owns a process
    group inside that session, so client teardown is local while runner timeout cleanup
    remains transitive.
  category: design
- id: T1
  kind: test
  state: active
  statement: Registry fixtures reject malformed IDs, duplicate cases, duplicate links,
    unexpected layers, and executable files omitted from the registry.
  layers:
  - tooling-unit
- id: T2
  kind: test
  state: active
  statement: Manifest discovery is byte-stable and a sentinel proves that no test
    body or Isabelle process ran.
  layers:
  - tooling-unit
- id: T3
  kind: test
  state: active
  statement: The runner propagates assertion failure, timeout, interrupt, and fixture
    failure to a nonzero exit and always reaps its child process.
  layers:
  - tooling-unit
- id: T4
  kind: test
  state: active
  statement: The retained public workflows pass through the registry against the selected
    real Isabelle server.
  layers:
  - e2e
- id: T5
  kind: test
  state: active
  statement: The maintained concurrency, duplicate-session, and Scala-bridge reproductions
    run as registered focused cases selectable by name or plan ID.
  layers:
  - e2e
- id: T6
  kind: test
  state: active
  statement: A fixture proves that filtered runs remain diagnostic only and cannot
    become completion evidence.
  layers:
  - tooling-unit
- id: T7
  kind: test
  state: active
  statement: Client close and timeout reap the complete client process group while
    its identity remains pinned, unsafe cleanup after external reap is refused, and
    runner timeout reaps a client-owned subgroup within the worker session.
  layers:
  - tooling-unit
spec_refinements: []
done_command:
- tools/planning-gate
- done
---

Keep the Python black-box runner and cases for retained server behavior: initialization, tool listing/calling, readiness, declaration notifications, cancellation, EOF/shutdown, theory load/check/unload, discovery and documentation.

Use tiny ML fixture tools for concurrency/output checks. Remove cases whose asserted contract is retired. Run the e2e blocking layer with the selected Isabelle installation and private worktree state. Do not report source inspection as a live pass.
