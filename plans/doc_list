doc_list — retained server obligations
==================================================

Status: retained contract; current carve-out verification pending.

List documentation from the existing Scala Doc_Catalog. Preserve shipped documentation, session document variants and existing filtering. Keep current readiness behavior.

Verification obligations
------------------------

D1 Use Sessions.Structure document_variants as already parsed variant names and map every name to its source session.

Q1 The shared documentation implementation is Doc_Catalog in mcp/src/doc_catalog.scala.

D2 Use Doc.contents with ML_Settings, retaining release-notes/manual fallback if settings or contents fail.

T1 [scala-unit] the join claim: every pdf manual in the bundled distribution resolves to a source session. assert at minimum the three naming shapes: "isar-ref" -> Isar_Ref (hyphen vs underscore), "logics-ZF" -> Logics_ZF (case + hyphen), "main" -> Main (name collision with the theory Main — the join is over doc sessions only, so no ambiguity).

T2 [scala-unit] variant parsing (D1, revised 2026-07-14 during implementation): Sessions.Structure already parses document_variants into List[Document_Build.Document_Variant] with .name stripped of any "=tags" suffix (Info.document_variants) -- no manual ":"/"=" splitting needed, so this is no longer a claim about our own code. Retargeted at Doc_Catalog.join, the pure fold that maps every variant name to its session: a session with variant names ["a", "b"] (as "a:b=t" parses to) ends up with both "a" -> sess and "b" -> sess in the map.

T3 [scala-unit] plain entries: NEWS appears (release notes section) with a non-pdf path, rendered "plain", readable by doc_read.

T4 [scala-unit] filtering is probe-safe: pattern "isar*" returns exactly the isar-ref entry; pattern "zzz*" returns an EMPTY listing with ok status, not an error.

T5 [bridge] full chain: tools/list advertises the row above; tools/call doc_list over a live server returns a listing containing isar-ref with its source session named.
