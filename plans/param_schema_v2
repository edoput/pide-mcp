param_schema_v2 — retained server obligations
==================================================

Status: retained contract; current carve-out verification pending.

Preserve the current parameter schema and mcp_tool syntax: primitive and structured types, required/optional arguments, defaults, descriptions and schema constraints remain validated in their existing owners. Preserve Scala JSON conversion and ML validation at invocation.

Keep the current typing/default/schema regression fixtures. Each ML tool automatically advertises the reserved optional String parameter context in its ordinary arguments. A declaration with its own context parameter must fail clearly. The framework consumes context as an opaque target URL, resolves it in ML, and removes it before invoking the selected root tool's unchanged #run function. Omitting context executes at the root. Do not add context to the eight Scala builtin schemas. Verify schema injection, declaration rejection, wrong-type handling and removal before tool-argument validation/invocation.

Verification obligations
------------------------
