doc_read — retained server obligations
==================================================

Status: retained contract; current carve-out verification pending.

Read documentation through the existing Scala Doc_Catalog. Preserve name versus session selection, section matching, plain-text/TeX handling and truthful PDF-only responses. Keep current readiness behavior.

Verification obligations
------------------------

D1 Keep the current line-anchored documentation heading extraction and its title/location handling; do not add source-navigation infrastructure.

D2 heading commands with markers/attributes (chapter \<open>..\<close>, section \<^marker>...): the doc sources use plain forms today; (a) must at least skip an optional target/marker before the title or tolerate extraction failure per-heading (drop the row, never crash the toc).

D3 Keep Doc_Catalog.window_lines = 200 and explicit range handling. Truncation reports how to narrow the read.

T1 [scala-unit] toc claim: doc_read {"name": "isar-ref"} returns headings from ALL chapter files of Isar_Ref (assert > 40 rows, at least one row per .thy file, every row carrying file + line, levels include chapter AND section).

T2 [scala-unit] section extraction: a pinned section of a bundled manual (pick one at implementation time and hard-pin title + a contained phrase) returns text containing the phrase and NOT containing the following same-level heading's title.

T3 [scala-unit] search-not-compile: an ambiguous `section` substring returns the candidate rows, not text; an unknown section and an unknown `name` are ok-status "no ..." replies, not errors.

T4 [scala-unit] plain entries: doc_read {"name": "NEWS", "lines": "1-40"} returns exactly that window; `section` on NEWS is an argument error; section+lines together is an argument error.

T5 [scala-unit] truncation (D3): a chapter-level section read truncates at the window with the "narrow" note.

T6 [scala-unit] the D1a line-initial assumption (only if (a) ships): every heading command occurrence in the bundled Isar_Ref sources is found by the scanner — count(scanner) equals a raw token-level or grep-established count pinned at implementation time. this is the canary for distribution upgrades reformatting the docs.

T7 [bridge] full chain: the lookup workflow over a live server — doc_list -> doc_read {"name": "isar-ref"} (toc) -> doc_read {name, section} (text).
