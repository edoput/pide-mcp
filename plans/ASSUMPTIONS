assumption registry (GENERATED -- do not edit)
=============================================

Regenerate:  tools/planning-gate registry generate
Check:       tools/planning-gate registry check

One row per assumption DECLARED in a plan file. The ID is the address a
test cites: MUnit cases attach structured tags, and Isabelle theories
declare spec_test metadata. An ID that no test cites is
an unchecked assumption; the gate reports the count.

ID                      LAYER                     STATEMENT
----------------------  ------------------------  ---------
check_theory#T1         bridge                    Load a valid fixture, edit it to introduce an error, check and observe that error, then fix and check successfully.
check_theory#T2         bridge                    Checking a never-loaded filesystem theory succeeds when the source is valid.
check_theory#T3         bridge                    Warnings remain diagnostics without becoming fatal errors; errors make checking fail.
check_theory#T4         e2e                       The public tool workflow loads, edits and checks a theory and reports the changed source accurately.
connection_kernel#A1    scala-unit                One connection observes one immutable validated policy snapshot; changing process configuration cannot partially reconfigure an active connection.
connection_kernel#A2    e2e,scala-unit            One stdio server process serves one logical MCP client and owns one Isabelle application instance for the lifetime of that connection.
connection_kernel#I1    scala-unit                DataPlane, RevisionRules, RequestScheduler, McpApplication, and InvariantViolationPolicy are narrow replaceable ports selected only at the composition
connection_kernel#I2    scala-unit                Production uses a bounded concurrent scheduler with immediate rejection and no application waiting queue, while tests can use a deterministic sequenti
connection_kernel#D1    unstated                  Client-visible protocol guarantees live in the specification; ownership, package boundaries, and execution strategy live in this implementation plan.
connection_kernel#D2    unstated                  Lifecycle, cancellation, ping, admission, and completion bookkeeping run on the connection control path and do not consume application worker capacity
connection_kernel#D3    unstated                  Policy is a nested Scala value with named construction, validated opaque leaf types, no correctness-affecting defaults, and one options-to-policy comp
connection_kernel#D4    unstated                  Registry invariant detection is fixed inside the connection kernel; FailFast, LogAndClose, and MarkBroken reactions are replaceable control-plane poli
connection_kernel#D5    unstated                  The implementation remains on MCP revision 2025-03-26 and accepts JSON-RPC batches only because that revision requires batch reception; no product beh
connection_kernel#D6    unstated                  Every concurrently callable Scala-to-ML round trip has a request-unique internal route; a reply completes only that route, and cancellation or shutdow
connection_kernel#Q1    unstated                  Which MCP revision and batch behavior does this remediation target?
connection_kernel#T1    scala-unit                Protocol fixtures cover valid and invalid requests, notifications, IDs, parameters, malformed JSON, and MCP 2025-03-26 wire responses without starting
connection_kernel#T2    e2e,scala-unit            A lifecycle transition table accepts only initialize in Fresh, initialized notification and ping in AwaitingInitialized, normal operations in Ready, a
connection_kernel#T3    scala-unit                Saturating maxInFlight admits exactly the configured bound, immediately returns structured overload errors for additional requests, creates no waiting
connection_kernel#T4    bridge,e2e,ml-unit,scala-unit  Cancellation during saturation reaches cooperative Scala and ML work, promptly removes and resolves the Scala bridge route, emits no response when can
connection_kernel#T5    scala-unit                Success, application error, exception, client cancellation, timeout, and shutdown each produce exactly one recorded terminal disposition and never mor
connection_kernel#T6    scala-unit                A second completion attempt and an unknown or forged completion token emit an invariant event, never emit a second response, and exercise FailFast, Lo
connection_kernel#T7    e2e,scala-unit            EOF with zero, one, and many in-flight requests drains replies including reverse completion order until one configured deadline, then cancels remainin
connection_kernel#T8    scala-unit                Contract suites run unchanged against stdio and scripted data planes and against bounded-concurrent and deterministic-sequential schedulers.
connection_kernel#T9    e2e,scala-unit            MCP Ready can coexist with Isabelle NotReady, Ready, or Failed; lifecycle acceptance is unchanged while application results truthfully report backend
connection_kernel#T10   e2e,scala-unit            Batch fixtures cover mixed requests and notifications, notification-only and empty batches, initialize-in-batch rejection, per-element admission limit
connection_kernel#T11   bridge,scala-unit         Concurrent tool catalogues, tool execution and context checks retain request-unique routes; distinguishable replies cannot cross, and cancellation lea
connection_kernel#T12   bridge                    Concurrent load, check, and unload operations are serialized around PIDE document mutation, cancellation while waiting is interruptible, and backend s
context_locator#A1      ml-unit                   Theory_Data attached to a registry-root theory inherits resolver declarations from its theory imports and can safely store the resolver functions need
context_locator#A2      bridge,ml-unit            A loaded theory produces a global Proof.context when its target is resolved.
context_locator#A3      bridge                    The server can bootstrap a private live PIDE wrapper importing exactly the -T selected theory, hold its document requirement until shutdown, and obser
context_locator#I1      bridge,ml-unit            MCP_Context_Locator is the sole owner of locator parsing, canonical printing, resolver registration, and resolution to Proof.context.
context_locator#D1      unstated                  A context locator is an address for an existing Isabelle context, not a third context kind beside theory, Proof.context, and Context.generic.
context_locator#D2      unstated                  Scala transports target URLs opaquely; ML owns the current parser and resolver extension mechanism. Do not redesign resolver result types in this clea
context_locator#D3      unstated                  MCP_Context_Locator stores named resolver functions in Theory_Data on the registry-root theory; MCP_Tools registers the theory resolver through ordina
context_locator#D4      unstated                  Every startup creates one private temporary live PIDE wrapper importing exactly the -T theory, uniformly for image and source imports. Its context sup
context_locator#D5      unstated                  The root catalogue selects names, activation and actual ML tool implementations; an invocation target supplies only the execution Proof.context.
context_locator#D6      unstated                  Every ML tool automatically advertises optional String context in ordinary arguments. The framework resolves its URL in ML, strips it before invoking
context_locator#Q1      unstated                  May an MCP client change the registry-root theory after the connection becomes Ready?
context_locator#T1      ml-unit                   Theory and extension locators round-trip through the existing ML parser/printer; malformed forms fail before resolver invocation.
context_locator#T2      ml-unit                   A resolver registered in an imported theory is available in its descendant, an unrelated theory cannot use it, and duplicate resolver-kind registratio
context_locator#T3      bridge,ml-unit            Theory and extension targets resolve at execution; unknown kinds and unknown theories return operation errors.
context_locator#T4      bridge,ml-unit,scala-unit  ML tool schemas include optional String context, builtin schemas do not; declaring a tool-owned context parameter fails. Scala transports extension UR
context_locator#T5      bridge,e2e,ml-unit,scala-unit  tools/list uses the fresh root catalogue; tools/call selects the root tool value and executes it in the target, including when the target declares a c
context_locator#T6      bridge,e2e                Starting with -T Scratch owns a private live PIDE wrapper importing exactly Scratch until shutdown, observes successful final end before readiness, an
doc_list#D1             unstated                  Use Sessions.Structure document_variants as already parsed variant names and map every name to its source session.
doc_list#Q1             unstated                  The shared documentation implementation is Doc_Catalog in mcp/src/doc_catalog.scala.
doc_list#D2             unstated                  Use Doc.contents with ML_Settings, retaining release-notes/manual fallback if settings or contents fail.
doc_list#T1             scala-unit                the join claim: every pdf manual in the bundled distribution resolves to a source session. assert at minimum the three naming shapes: "isar-ref" -> Is
doc_list#T2             scala-unit                variant parsing (D1, revised 2026-07-14 during implementation): Sessions.Structure already parses document_variants into List[Document_Build.Document_
doc_list#T3             scala-unit                plain entries: NEWS appears (release notes section) with a non-pdf path, rendered "plain", readable by doc_read.
doc_list#T4             scala-unit                filtering is probe-safe: pattern "isar*" returns exactly the isar-ref entry; pattern "zzz*" returns an EMPTY listing with ok status, not an error.
doc_list#T5             bridge                    full chain: tools/list advertises the row above; tools/call doc_list over a live server returns a listing containing isar-ref with its source session
doc_read#D1             unstated                  Keep the current line-anchored documentation heading extraction and its title/location handling; do not add source-navigation infrastructure.
doc_read#D2             unstated                  heading commands with markers/attributes (chapter \<open>..\<close>, section \<^marker>...): the doc sources use plain forms today; (a) must at least
doc_read#D3             unstated                  Keep Doc_Catalog.window_lines = 200 and explicit range handling. Truncation reports how to narrow the read.
doc_read#T1             scala-unit                toc claim: doc_read {"name": "isar-ref"} returns headings from ALL chapter files of Isar_Ref (assert > 40 rows, at least one row per .thy file, every
doc_read#T2             scala-unit                section extraction: a pinned section of a bundled manual (pick one at implementation time and hard-pin title + a contained phrase) returns text contai
doc_read#T3             scala-unit                search-not-compile: an ambiguous `section` substring returns the candidate rows, not text; an unknown section and an unknown `name` are ok-status "no
doc_read#T4             scala-unit                plain entries: doc_read {"name": "NEWS", "lines": "1-40"} returns exactly that window; `section` on NEWS is an argument error; section+lines together
doc_read#T5             scala-unit                truncation (D3): a chapter-level section read truncates at the window with the "narrow" note.
doc_read#T6             scala-unit                the D1a line-initial assumption (only if (a) ships): every heading command occurrence in the bundled Isar_Ref sources is found by the scanner — count(
doc_read#T7             bridge                    full chain: the lookup workflow over a live server — doc_list -> doc_read {"name": "isar-ref"} (toc) -> doc_read {name, section} (text).
list_sessions#D1        scala-unit                the session structure computed once at server startup (from the same -d dirs) enumerates every session with chapter, description, theories and source
list_sessions#T1        unstated                  D1 (above).
list_sessions#T2        scala-unit                All known sessions are listed deterministically with chapter, heap and theory-count columns.
list_sessions#T3        scala-unit                heap_present reflects store.get_session(name).defined at the time of the call.
list_sessions#T4        e2e                       Public list_sessions reports the selected base session and catalogue entries.
list_theories#T1        bridge,scala-unit         [scala-unit] For a known session, list_theories returns its sorted catalogue theory names.
list_theories#T2        scala-unit                Unknown session returns an error naming it; a known session with no theories returns an empty successful report.
list_theories#T3        scala-unit                The returned names match the configured session catalogue, without promising tier or source-location fields.
list_theories#T4        e2e                       Public list_theories uses the requested session and returns its theory names.
load_theory#T1          bridge                    A well-formed theory under master_dir loads successfully.
load_theory#T2          bridge                    A broken theory reports an error with positioned messages.
load_theory#T3          bridge                    Session-qualified names resolve without master_dir through the startup session structure.
load_theory#T4          bridge                    Loading an unchanged theory succeeds.
load_theory#T6          e2e                       Load a filesystem fixture, edit it, and check the edited file through the retained public tools.
mcp_tool_registry#T1    ml-unit                   HOL-safe MCP registration permits ordinary datatype declarations and simplifier proofs with MCP both before and after another HOL theory in the import
mcp_tool_registry#T2    ml-unit                   Capture-form tools return printed output, join output with failures, expose all validated declared arguments including defaults, reject undeclared arg
pide_bridge#A1          scala-unit                A bridge call ID is generated independently, is never derived from or deliberately reused as a client JSON-RPC request ID, and is never exposed throug
pide_bridge#I1          bridge,scala-unit         The same PideBridge runs over scripted and production PideTransport implementations, while application services depend only on the typed bridge port.
pide_bridge#D1          unstated                  The internal bridge revision is independent of the negotiated public MCP protocol revision and is present in every request, cancellation, and result e
pide_bridge#D2          unstated                  One revision-1 bridge command and result function serve typed operations. Every hello/call requires root_node, root_command and root_exec beside theor
pide_bridge#D3          unstated                  The bridge control plane owns call IDs, admission, deadlines, pending completion, cancellation, stop, and invariant events; the transport owns only pr
pide_bridge#D4          unstated                  Client cancellation and the connection deadline remain the public request race owners; the bridge safety deadline is a backstop for direct and non-con
pide_bridge#D5          unstated                  MCP_Tools defines the extensible ML operation registry and common executor. Handlers are inherited through the configured root and descendants can add
pide_bridge#D6          unstated                  Reply limits count serialized YXML bytes and retain ML pre-publication and Scala pre-decoding protection. Request-byte enforcement was absent in the b
pide_bridge#D7          unstated                  PideTransport reports byte delivery and termination but does not own Headless.Session.stop; the composition root owns Isabelle session termination and
pide_bridge#D8          unstated                  PideBridge.call returns Either[BridgeFailure, A]; an operation-domain error remains a successfully decoded A, while transport, protocol, remote-execut
pide_bridge#Q1          unstated                  What separate serialized request and reply byte limits admit the committed legitimate-operation corpus while bounding accidental or hostile bridge-env
pide_bridge#T1          bridge,scala-unit         Two distinguishable calls released in reverse order receive their own replies exactly once through the same bridge registry.
pide_bridge#T2          scala-unit                A wrong-operation, wrong-version, malformed, or invalid-status reply with a known call ID atomically fails that matching call with ProtocolError and c
pide_bridge#T3          bridge,ml-unit,scala-unit  Cancellation before dispatch sends no call; cancellation after dispatch sends one ordered cancel. Races produce one terminal outcome and leave no pend
pide_bridge#T4          bridge,scala-unit         Timeout/result races produce one TimedOut or successful outcome, interrupt admitted ML work, and leave no pending entry, deadline, or ML route.
pide_bridge#T5          scala-unit                A transport send failure and later transport termination each fail every affected call with TransportFailed and leave no pending entry or deadline.
pide_bridge#T6          bridge,heap,scala-unit    Stop/result races and repeated stop calls admit no post-stop work; stop acknowledges only after Scala pending entries and deadlines plus ML routes and
pide_bridge#T7          scala-unit                Saturating maxPending admits exactly the configured bound, creates no waiting queue, rejects the next call immediately, and admits a later call after
pide_bridge#T8          bridge,ml-unit,scala-unit  Unsupported revisions, unknown operations, malformed envelopes, and invalid status or payload combinations return a correlated ProtocolError instead o
pide_bridge#T9          bridge,ml-unit            Retained tools, run_tool and check_context operations register in MCP_Tools, reject duplicate registration and use the common executor and production
pide_bridge#T10         bridge,scala-unit         Bootstrap creates the private live PIDE wrapper importing exactly the -T selected image/source theory, and waits for successful final end before bound
pide_bridge#T12         bridge,ml-unit,scala-unit  A serialized reply of exactly maxReplyBytes succeeds, while a larger operation result becomes a small correlated TooLarge reply before protocol public
plan_format#A1          tooling-unit              A plan has one canonical machine-readable value for every field used by a gate.  Markdown may explain a field but cannot override it.
plan_format#A2          tooling-unit              YAML is parsed safely and duplicate keys are rejected before schema or semantic validation.
plan_format#A3          tooling-unit              A plan remains readable without custom rendering: the frontmatter is short structured data and the body is ordinary Markdown.
plan_format#I1          tooling-unit              Existing qualified plan IDs and test citations survive migration byte for byte; format migration does not change ownership.
plan_format#D1          unstated                  YAML frontmatter plus JSON Schema is the selected interchange format. PyYAML, jsonschema, and pytest are host-side development dependencies.
plan_format#D2          unstated                  Architecture decisions may be referenced from plans as ADR IDs, executable examples may be referenced as Gherkin or test IDs, and concurrency models m
plan_format#T1          tooling-unit              A valid v1 fixture parses to the expected typed plan object.
plan_format#T2          tooling-unit              Missing frontmatter, duplicate keys, unknown fields, unsafe YAML values, malformed IDs, and shell-string commands are each rejected by an isolated fix
plan_format#T3          tooling-unit              Unknown dependencies and paths outside the repository are rejected with the plan path and field name.
plan_format#T4          tooling-unit              A v1 plan cannot repeat an A/I/D/Q/T declaration in its Markdown body.
plan_format#T5          tooling-unit              Migrating a fixture from legacy prose to v1 preserves every qualified claim ID and produces byte-stable generated registries.
plan_label_schema#A1    tooling-unit              Every ACTIVE A record represents behavior that must be verified before its owner plan is done.
plan_label_schema#A2    tooling-unit              A MOOT or SUPERSEDED A record keeps its stable ID and an auditable reason; it does not receive a false verification link.
plan_label_schema#I1    tooling-unit              An ACTIVE I record is verified by its owner exactly once and can be reused through qualified references without duplicate declarations.
plan_label_schema#D1    unstated                  A D record is structurally checked, but test completeness is not inferred from the historically ambiguous D prefix.
plan_label_schema#D2    unstated                  A behavioral historical D becomes A and a concrete verification requirement becomes T; the migration records the old qualified ID as an alias or super
plan_label_schema#T1    tooling-unit              ACTIVE, MOOT, and SUPERSEDED A/I fixture records validate only with their required rationale or target fields.
plan_label_schema#T2    tooling-unit              A dependency D with an unknown target is rejected; DESIGN and DETAIL D records do not require test metadata.
plan_label_schema#T3    tooling-unit              An OPEN Q prevents done; a RESOLVED Q without a date and resolution is rejected.
plan_label_schema#T4    tooling-unit              A new ACTIVE A/I/T record with no explicit layer is rejected.
plan_label_schema#T5    tooling-unit              Downstream references to an I record resolve to one owner and duplicate declarations are rejected.
plan_label_schema#T6    tooling-unit              Migration fixtures preserve qualified IDs and reject an unreviewed semantic conversion between A, D, and T.
planning_gate#A1        tooling-unit              One invocation is the only project command that may establish done; static metadata generation alone cannot.
planning_gate#A2        tooling-unit              A dirty working tree is reported with exact paths and left to explicit human review locally.
planning_gate#I1        scala-unit,tooling-unit   Each build, discovery, static-check, and test-layer step remains callable independently for diagnosis and agent iteration.
planning_gate#D1        unstated                  CI integration is deferred; the host command is designed for later reuse without making CI a condition of this plan.
planning_gate#T1        tooling-unit              Static fixtures reject missing A/I verification, missing T coverage in any layer, open Q/refinement state, dangling D dependencies, unknown IDs, and u
planning_gate#T2        tooling-unit              Generating every discovery manifest without executing tests cannot make `done` succeed.
planning_gate#T3        tooling-unit              Each registered command-step failure makes `done` exit nonzero and identifies its failed step.
planning_gate#T4        tooling-unit              A filtered or reduced-layer invocation is rejected by the completion command while remaining available through diagnostic subcommands.
planning_gate#T5        tooling-unit              Dirty paths are printed; a changed HEAD or tracked content during execution invalidates the run.
planning_gate#T6        scala-unit                The shutdown-drain regression fixture is always selected by the full Scala suite; while red it blocks `done` through the layer-failure rule, and after
planning_gate#T7        scala-performance,scala-unit,tooling-unit  Documentation TOC classification fixtures distinguish functional and performance checks;
planning_gate#T8        bridge,e2e,heap,ml-unit,scala-performance,scala-unit,tooling-unit  An orchestration fixture requires every registered layer;
planning_gate#T9        tooling-unit              Static completion rejects a missing or stale legacy label-migration audit and reports the exact regeneration command.
python_e2e#A1           e2e                       A procedural process test exercises the shipped process and public protocol, not an in-process fake or an LLM agent.
python_e2e#A2           tooling-unit              Metadata discovery is deterministic and has no process, network, prover, or test-body side effects.
python_e2e#I1           tooling-unit              All cases share one client, timeout, teardown, metadata, and result-reporting implementation instead of copying process code between scripts.
python_e2e#D1           unstated                  Agent usability is a separate evaluation concern; process-test links certify only deterministic procedural behavior.
python_e2e#D2           unstated                  Each worker owns a process session and each JSON-RPC client owns a process group inside that session, so client teardown is local while runner timeout
python_e2e#T1           tooling-unit              Registry fixtures reject malformed IDs, duplicate cases, duplicate links, unexpected layers, and executable files omitted from the registry.
python_e2e#T2           tooling-unit              Manifest discovery is byte-stable and a sentinel proves that no test body or Isabelle process ran.
python_e2e#T3           tooling-unit              The runner propagates assertion failure, timeout, interrupt, and fixture failure to a nonzero exit and always reaps its child process.
python_e2e#T4           e2e                       The retained public workflows pass through the registry against the selected real Isabelle server.
python_e2e#T5           e2e                       The maintained concurrency, duplicate-session, and Scala-bridge reproductions run as registered focused cases selectable by name or plan ID.
python_e2e#T6           tooling-unit              A fixture proves that filtered runs remain diagnostic only and cannot become completion evidence.
python_e2e#T7           tooling-unit              Client close and timeout reap the complete client process group while its identity remains pinned, unsafe cleanup after external reap is refused, and
readiness#A1            unstated                  initialize does not depend on the backend.
readiness#T1            scala-unit                drive Handler with a readiness thunk fixed at Not_Ready("building"); assert initialize returns the normal capabilities reply, and that the thunk's bac
readiness#A2            unstated                  tools/list answers while not ready, with the builtin table.
readiness#T2            scala-unit                same fixed-Not_Ready handler; assert the reply lists exactly MCP_Server.all_builtin_names and no ml rows, and that ml_tools() was not called.
readiness#A3            unstated                  a prover-backed tools/call while not ready is an isError result, not a json-rpc error, and names the state.
readiness#T3            scala-unit                assert isError true, the text contains the progress string, and the response carries the request id (a result, not an error object).
readiness#A4            unstated                  Failed is reported distinctly from Not_Ready.
readiness#T4            scala-unit                readiness thunk at Failed("boom"); assert the tools/call text says failed and carries "boom".
readiness#A5            unstated                  the transition is observable without restarting the handler.
readiness#T5            scala-unit                a mutable thunk flipped from Not_Ready to Ready(Fake_Backend) between two tools/call lines; assert the first is isError and the second succeeds. This
readiness#A7            unstated                  the real server serves stdin before its session is up.
readiness#T7            bridge                    Run the real server against a delayed startup and observe initialize and tools/list before prover readiness, then successful retained tool execution.
readiness#A8            unstated                  Tool declaration notifications survive asynchronous startup.
readiness#T8            bridge                    Once Ready, declaration of a tool emits notifications/tools/list_changed; notifications remain deliverable while work is pending.
readiness#A9            unstated                  Closing stdin during startup exits cleanly.
readiness#T9            bridge                    Closing stdin during startup exits cleanly without leaving a prover process.
readiness_catalog#A6    unstated                  Shutdown while catalogue construction is pending exits cleanly.
readiness_catalog#T6    unstated                  Close the input while startup/catalogue work is pending and verify bounded process shutdown.
search_sources#T1       scala-unit                Matches are session-qualified theory names, sorted deterministically.
search_sources#T2       scala-unit                No matching name returns a successful no-matches report.
search_sources#T3       scala-unit                Search sees theory_map entries contributed by the configured session catalogue.
search_sources#T4       scala-unit                Pattern matching is literal substring matching, including punctuation; no regex interpretation occurs.
search_sources#T5       scala-unit                An empty pattern returns a successful no-matches report without scanning source content.
search_sources#T6       e2e                       Public search_sources returns names matching the supplied substring from the catalogue.
session_dirs_errors#A1  unstated                  Root entries can be inspected for duplicates before constructing the complete session structure.
session_dirs_errors#T1  scala-unit                fixture with two colliding roots; assert check() returns a Collision naming both root paths and lines, and does not throw.
session_dirs_errors#A2  unstated                  Session entry positions identify the ROOT file and line for diagnostics.
session_dirs_errors#T2  scala-unit                assert both are populated for a parsed Session_Entry.
session_dirs_errors#A3  unstated                  Comparing user directory roots with registered roots distinguishes their provenance.
session_dirs_errors#T3  scala-unit                a -d root colliding with a REGISTERED component session is classified From_Components, not From_Dir, and the message says "rename yours" rather than d
session_dirs_errors#A6  heap                      The pre-scan runs only when custom directories are supplied; retain bounded startup behavior.
session_structure#A1    scala-unit                Session structure and dependency data support list_sessions and list_theories without loading every theory into the document.
session_structure#A2    scala-unit                Session discovery uses the existing startup catalogue and does not repeatedly rebuild it per query.
session_structure#A3    bridge,scala-unit         [scala-unit] Theory resolution preserves image, loaded and filesystem precedence for retained callers.
session_structure#A6    bridge                    load_theory resolves session-qualified theory names without master_dir.
session_structure#A7    bridge,scala-unit         [scala-unit] Image-name lookup and image-unload rejection remain correct.
session_structure#A8    bridge,scala-unit         [scala-unit] The loaded tier follows document loading and unloading.
unload_theory#T1        bridge                    Load a filesystem fixture, unload it, then load it again successfully.
unload_theory#T2        scala-unit                Unloading an unavailable or image theory returns a clean error without damaging the session.
unload_theory#T5        e2e                       Exercise load, unload and reload through the public tools.
verification_matrix#A1  tooling-unit              The verification matrix is static coverage metadata, not proof that any test executed or passed.
verification_matrix#A2  tooling-unit              Discovery does not start the theorem prover, spawn a server, or evaluate test bodies.
verification_matrix#I1  tooling-unit              All producers use one versioned relation and test-record contract, so the planning gate does not special-case claim semantics per language.
verification_matrix#D1  unstated                  `verifies` targets ACTIVE A/I records; `covers` targets T records.
verification_matrix#D2  unstated                  Required layers use all-layer semantics: every named layer has a distinct registered test identity.
verification_matrix#T1  tooling-unit              Unknown IDs, relation-kind mismatches, duplicate identities, duplicate links, and wrong-layer links are rejected by isolated fixtures.
verification_matrix#T2  tooling-unit              An ACTIVE A/I record missing one required verifying layer is reported as uncovered.
verification_matrix#T3  tooling-unit              A T record naming two required fixture layers is rejected when only one layer has a covering test and accepted when distinct tests cover both.
verification_matrix#T4  tooling-unit              Matrix output is byte-stable under producer and discovery reordering.
verification_matrix#T5  tooling-unit              A missing producer and an unknown layer fail instead of reducing the required layer set.
verification_matrix#T6  tooling-unit              Legacy debt fixtures reject new rows, allow reviewed existing rows, and remove a row once its relationship is present.
verification_matrix#T7  scala-unit                MUnit discovery emits `covers`, preserves every migrated link, and rejects the removed `discharges` relation.
verification_matrix#T8  tooling-unit              Isabelle manifest fixtures preserve session, theory, test identity, source freshness, layer, and spec_test links while proving that a citation or an o
