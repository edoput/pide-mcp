session_structure — retained server obligations
==================================================

Status: retained contract; current carve-out verification pending.

Retain Sessions.load_structure/deps, theory_map and name resolution for load/check and discovery. Image, loaded and filesystem distinctions remain useful for theory discovery and the image-unload guard. Delete source navigation and resource snapshot readers.

Verification obligations
------------------------

A1 [scala-unit] Session structure and dependency data support list_sessions and list_theories without loading every theory into the document.

A2 [scala-unit] Session discovery uses the existing startup catalogue and does not repeatedly rebuild it per query.

A3 [bridge] [scala-unit] Theory resolution preserves image, loaded and filesystem precedence for retained callers.

A6 [bridge] load_theory resolves session-qualified theory names without master_dir.

A7 [bridge] [scala-unit] Image-name lookup and image-unload rejection remain correct.

A8 [bridge] [scala-unit] The loaded tier follows document loading and unloading.
