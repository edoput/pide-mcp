mcp_tool_command — retained server obligations
==================================================

Status: retained contract; current carve-out verification pending.

Keep the existing mcp_tool declaration language, mcp_test, parameter/schema validation and mcp_tools activation attributes. No declaration-language redesign belongs to this cut. Isabelle bundles and local Isar activation remain theory-author mechanisms.

ML tool implementations receive a Proof.context and tool arguments. The server selects the implementation from the fresh startup-root catalogue and resolves the opaque invocation target in ML. Preserve command forms, typed argument conversion, compile-time rejection, output capture, bundles and registration tests.

Every ML tool automatically advertises optional String context in ordinary
arguments. The framework resolves that target URL in ML and strips context
before calling the root-selected tool's unchanged #run. Absence defaults to the
root. Reject declaration of a tool-owned context parameter clearly; authors do
not resolve targets themselves. Scala builtins do not gain context.

Verification obligations
------------------------
